namespace Projects.Service.Common
{
    public static class ErrorMessages
    {
        public static string InvalidProjectId = "invalid project id";
        public static string AlreadyCollaborator = "the receiver is already a collaborator";
        public static string InvitationExists = "the invitation has already been sent";
    }
}
